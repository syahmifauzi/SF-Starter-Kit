$(document).ready(function() {

  console.log("Let's Start!");

  $('.lets-start').funnyText({
    // All defaults setting in funnyText..
		speed: 700,
		fontSize: '7em',
		color: 'black',
		activeColor: '#fff',
		borderColor: 'black',
    direction: 'both' // can be vertical or horizontal
  });

});
